import 'package:flutter/material.dart';

class MyDashBoard extends StatelessWidget {
  static const String routeName = '/mydashboard';
  const MyDashBoard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
