import 'package:flutter/material.dart';

class MyLateArrival extends StatelessWidget {
  static const String routeName = '/mylatearrival';
  const MyLateArrival({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

